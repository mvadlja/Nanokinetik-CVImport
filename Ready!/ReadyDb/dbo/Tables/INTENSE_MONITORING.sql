CREATE TABLE [dbo].[INTENSE_MONITORING] (
    [intense_monitoring_PK] INT IDENTITY (1, 1) NOT NULL,
    [name]                  INT NULL,
    CONSTRAINT [PK_INTENSE_MONITORING] PRIMARY KEY CLUSTERED ([intense_monitoring_PK] ASC)
);

